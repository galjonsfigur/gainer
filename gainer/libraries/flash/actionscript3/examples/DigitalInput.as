package {	import flash.display.Sprite;	import flash.events.Event;	import flash.text.*;	import gainer.*;	public class DigitalInput extends Sprite {		private var _gnr:Gainer;		var textBoxes:Array;		public function DigitalInput() {			// Gainerのインスタンスを生成する			_gnr = new Gainer("localhost", 2000, Gainer.MODE1, true);			textBoxes = new Array(4);			// アナログ入力の値を表示するために.fla側で配置したテキストフィールドを取得			for (var i:int = 0; i < 4; i++) {				textBoxes[i] = this["d" + i];			}			// I/Oモジュールの準備が完了した時に呼ばれるイベントハンドラをセット			_gnr.onReady = function() {				_start();			};		}		// I/Oモジュールの準備が完了したら以下を実行		private function _start():void {			// アナログ入力の連続取得を開始			_gnr.beginDigitalInput();			// ENTER_FRAMEイベントに対するイベントリスナをセット			addEventListener(Event.ENTER_FRAME, _onEnterFrame);		}		// 毎フレームごとにテキストフィールドを更新する		private function _onEnterFrame(e:Event):void {			for (var i:int = 0; i < _gnr.digitalInput.length; i++) {				textBoxes[i].text = _gnr.digitalInput[i];			}		}	}}